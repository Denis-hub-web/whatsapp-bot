# Server port (Render sets this automatically)
PORT=3000

# Directory to store WhatsApp session credentials
# On Render, use /data/auth_info (persistent disk mounted at /data)
# Locally, use ./auth_info
AUTH_DIR=./auth_info
